# Edge-level redirects (processed BEFORE _worker.js)
# See: https://developers.cloudflare.com/pages/configuration/redirects/
#
# WARNING: Do NOT add SPA fallback here (/* /index.html 200).
# The _worker.js handles SPA routing. A catch-all rewrite in _redirects
# would intercept /auth/callback and /api/* before the Worker sees them.

/favicon.ico  /favicon.svg  301
