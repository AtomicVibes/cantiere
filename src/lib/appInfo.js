// Central application metadata (single source - do not duplicate).
// Displayed in Settings > About. package.json remains the build version;
// APP_VERSION is the user-facing product version.
export const APP_NAME = 'Geometra';
export const APP_VERSION = '0.1';
export const APP_VERSION_LABEL = `v${APP_VERSION}`;
